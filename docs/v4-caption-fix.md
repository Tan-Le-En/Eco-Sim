# Results caption gap — still present after h-full min-h-280

The photo (img absolute inset-0) stops above the caption: visible white gap between photo bottom and caption bar.
Cause hypothesis: the figure (photo-plate) is h-full inside a grid row, but the grid column (.grid-cols-[46%_1fr]) doesn't stretch figure to full height; img covers figure height, caption sits at figure bottom — actually the gap suggests figure is taller than img area? No: img is absolute inset-0 so it fills figure. The gap is between img and caption because caption is absolute bottom-0 — wait, gap visible means img doesn't reach caption. So figure must be taller than img because img is absolute but figure height set by grid... Actually img absolute inset-0 DOES stretch to figure. The visible gap likely comes from img `h-full w-full object-cover` with figure height auto (min-h 280) — absolute img has no effect on figure height; figure height = min-h 280; img covers fully; caption absolute bottom... there should be no gap.
Real cause from screenshot: caption bar has its own top spacing? caption is absolute bottom-0 so no. The gap is white — it's part of the img not covering? Photo aspect: object-cover full coverage.
=> Likely the white strip is the figure's background showing because the img's absolute positioning is offset by... Check index.css .photo-plate maybe has `padding`! That's it — photo-plate class probably has padding (caption strip style copied). Inspect .photo-plate block.
